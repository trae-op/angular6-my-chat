import { TypeMessagePipe } from './type-message.pipe';

describe('TypeMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new TypeMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
